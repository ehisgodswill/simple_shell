#include "main.h"

/**
 * main - 
 * @argc: arguement count
 * @argv: argument vector, a strings that come after calling function
 * Return: 0 (Success) or -1 (Failure)
 */
int main(void)
{
	data shelldata;

	if (isatty())
	{
		/* code */
	}
	
	setdata(&shelldata, NULL);
	return (0);
}
